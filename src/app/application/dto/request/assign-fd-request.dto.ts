/**
 * DTO de requete pour assigner un disciple a un FD
 */
export interface AssignFDRequestDTO {
  fdId: string;
}

/**
 * DTO de requete pour assigner un role a un utilisateur
 */
export interface AssignRoleRequestDTO {
  role: string;
}
